const messageTemplates = require('./messageTemplates');
const notificationStatuses = require('./notificationStatuses');
const notificationTypes = require('./notificationTypes');

module.exports = {
	messageTemplates,
	notificationStatuses,
	notificationTypes,
};
